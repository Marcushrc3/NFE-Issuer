package com.example.nfe.domain;

/**
 * Lifecycle status of an emission. Only RECEIVED exists for now; further
 * states will be added when real issuance processing is implemented.
 */
public enum EmissionStatus {
    RECEIVED
}
